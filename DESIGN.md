# Design System

## Overview

Qt FusionとOS標準フォントを基盤にした、研究作業向けの高密度デスクトップUI。標準コントロールを優先し、見慣れたWindowsの操作感を維持する。

## Theme

- OS標準パレットを尊重し、ライト・ダーク双方で文字と背景のコントラストを確保する。
- アクセントは主要操作、選択、フォーカスに限定する。
- 成功、警告、失敗、処理中はアイコンまたは短いラベルを併記し、色だけで伝えない。

## Typography

- OS標準UIフォントを一貫して使用する。
- 見出し、説明、ラベル、数値の階層はサイズとウェイトで示す。
- パスやログは省略表示とツールチップ／選択可能な全文表示を組み合わせる。

## Layout

- 主要ワークフローは対象、設定、プレビュー、実行を一つの画面にまとめる。
- フォームのラベル幅とコントロール高さを揃え、最小44px相当の主要操作領域を確保する。
- 大量データはスクロール領域に閉じ込め、ウィンドウ全体を押し広げない。

## Components

- Primary button: 実行や保存など、画面に一つの主操作。
- Secondary button: 参照、再検出、キャンセル。
- Inline status: 入力検証、セル自動検出、処理状態をフォーム直下に表示。
- Summary panel: 原子数、元素、拘束、セル、出力形式を実行前に一覧表示。
- Progress state: 操作無効化と明示的な「解析中」表示を組み合わせる。

## Interaction

- ドラッグ＆ドロップとファイル選択を同等に扱う。
- 入力変更後の解析はバックグラウンドで行い、古い結果は採用しない。
- エラーは修正すべき項目の近くに表示し、成功後は生成物を自動的に開く。
- 不要な連続モーダルを避け、確認が必要な破壊的操作にのみ確認ダイアログを使う。
